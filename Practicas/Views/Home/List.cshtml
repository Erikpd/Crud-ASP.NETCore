@model IEnumerable<Practicas.Models.Alumnos>

@{
    ViewData["Title"] = "List";
}

<h2 align="center">Registro de compras</h2>
<br />

<table class="table">
    <thead class="bg-primary">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Clave)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Producto)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Cantidad)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Obsevaciones)
            </th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Clave)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Producto)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Cantidad)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Obsevaciones)
            </td>
            <td>
                <button class="btn btn-default">
                    @Html.ActionLink("Editar", "Update", new { id = item.Id })
                </button> |
                <button class="btn btn-default">
                    @Html.ActionLink("Detalles", "Details", new { id = item.Id })
                </button> |
                <button class="btn btn-default">
                    @Html.ActionLink("Borrar", "Delete", new { id = item.Id })
                </button>
            </td>
        </tr>
}
    </tbody>
</table>
<p align="center">
    <a asp-action="Create" class="btn btn-default">Nuevo Registro</a>
</p>